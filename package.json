{
  "name": "shipit-deploy",
  "version": "2.2.0",
  "description": "Set of deployment tasks for Shipit based on git and rsync commands. ",
  "scripts": {
    "install": "echo \"PACKAGE DEPREACTED: Use shipit-deploy\" && exit 1"
  }
}
